import 'package:flutter/material.dart';

class PaginaInternet extends StatelessWidget {
  const PaginaInternet({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Página de Internet'),
    );
  }
}
